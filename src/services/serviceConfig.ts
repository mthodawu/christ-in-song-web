
// Configuration for service behavior
export const serviceConfig = {
  // Use mock data instead of real API calls (useful for development without backend)
  useMockData: true,
  
  // API base URL
  apiBaseUrl: '/api',
  
  // Debug mode for additional logging
  debug: true
};

export default serviceConfig;
